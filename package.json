{
  "name": "streetscope-ai",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "build": "if [ -n \"$VERCEL\" ]; then npm run build --workspace frontend && cp -r frontend/.next .next; else npm run build --workspace backend && npm run build --workspace frontend; fi",
    "lint": "npm run lint --workspace frontend",
    "vercel-build": "npm run build --workspace frontend && cp -r frontend/.next .next"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
